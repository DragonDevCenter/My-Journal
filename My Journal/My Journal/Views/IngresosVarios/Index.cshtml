@model IEnumerable<My_Journal.Models.IngresosVario>

@{
    ViewData["Title"] = "Index";
}

<h1>Ingresos Varios</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Cantidad)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Descripcion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FechaVarios)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Cantidad)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Descripcion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FechaVarios)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdVarios">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdVarios">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdVarios">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
